package main.event;

public enum VoiceCommand implements SimulationEvent {
    // TODO This could be handled in a better way to add many commands, but that's not the point of the simulation
    PLAY_MORNING_PLAYLIST
}
